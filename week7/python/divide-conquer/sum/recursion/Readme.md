### Readme - Divide and Conquer - Sum - Recursion

Basic example to sum an array data structure of size `n` with recursion.

To run this example, simply `cd` to the app's root directory, and execute the following

```bash
python3 app.py
```