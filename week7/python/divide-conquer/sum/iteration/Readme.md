### Readme - Divide and Conquer - Sum - Iteration

Basic example to sum an array data structure of size `n` with iteration.

To run this example, simply `cd` to the app's root directory, and execute the following

```bash
python3 app.py
```