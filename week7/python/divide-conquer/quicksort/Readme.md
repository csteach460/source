### Readme - Divide and Conquer - Quicksort - Recursion

Basic example to sort an array data structure of size `n` with *quicksort*.

To run this example, simply `cd` to the app's root directory, and execute the following

```bash
python3 app.py
```